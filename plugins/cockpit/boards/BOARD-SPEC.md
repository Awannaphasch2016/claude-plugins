# The Move Board — structure spec (the sponsor's design; changes ONLY by his word)

Element roster and ORDER, fixed as of run 28b (2026-08-30):

1. h1 + stamp (invocation · graph read · estimates)
2. Δ strip (what changed since the previous run — teal box)
3. lens-quote (the standing lens)
4. stale banner ("this ordering died with its conversation")
5. the declared grammar table (single source of marks; audit line)
6. ① Pace panel — "how am I doing?" (fuel · pace · traction)
7. ② The Trajectory — "how is my performance affecting this race and
   the next?" (slope view; RED delivery pin; board tokens)
8. ③ The Horizon — queue × time, fused — "the overall view of the rest
   of the race" (cascade bars · glory sweep left of now · G2
   illustrative, marked)
9. ⚡ constraint band — ISOLATED, stacked directly on top of the grid
10. the quadrant grid (Next spine · Urgent-not-important · Neither)
11. discover strip (recent constraint moves)
12. footer ("THE PAGE ENDS HERE")

Rules bound to every regeneration:
- REGENERATE WHOLE from this spec + fresh facts; never patch blind.
- Removing/adding/reordering an element = a design change = the
  sponsor's explicit word first. No exceptions for "cleanups".
- Cross-page lifts: remap COLOR TOKENS to the board palette AND carry
  the standard svg sizing (min-width:700px;display:block;
  max-width:100%;height:auto) — attributes are part of the mark.
- Before publish: diff against the previous file; any visual change not
  traceable to the sponsor's words or fresh facts is a defect, not a
  bonus.
- All numbers from ONE fact model per run; unratified dates stay out of
  the instruments; every mark from the grammar table.

## Approved baseline
Run 29 (2026-08-30 09:30 BKK) was ruled RIGHT by the sponsor ("this looks
right to how I want it"). archive/APPROVED-run29-baseline.html is the
golden copy: every future regeneration diffs against IT — structure
identical, only facts and run-stamps may differ. Divergence from the
baseline's structure without the sponsor's word is a defect by definition.
